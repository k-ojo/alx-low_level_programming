#include <stdio.h>

/**
 * main- a program to outup the base 10 digits
 *
 * Return: 0 if success
 */
int main(void)
{
	char i;

	for (i = 0; i < 10; i++)
		printf("%i", i);

	putchar('\n');
	return (0);
}
